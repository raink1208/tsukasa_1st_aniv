export interface StreamInfo {
  id: string,
  url: string,
  title: string,
  thumbnail: string,
  startAt: string,
  endAt: string,
  scheduledStartAt: string,
  lateTime: number,
  streamTime: number,
}
